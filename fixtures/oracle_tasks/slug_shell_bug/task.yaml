task_id: slug_shell_bug
title: Fix slug rendering for shell validator
prompt: |
  render_slug should replace spaces with hyphens and lowercase the value.
  Update only app.py so the shell oracle passes.
workspace: workspace
allowed_files:
  - app.py
  - oracle.sh
required_checks:
  - shell-oracle
doubt_points:
  - Keep the implementation simple.
oracles:
  - name: shell-oracle
    kind: shell
    command: ./oracle.sh
adapter: replay
replay_file: replay.json
retry_budget: 2
tags:
  - shell
