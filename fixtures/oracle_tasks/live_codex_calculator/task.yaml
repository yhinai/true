task_id: live_codex_calculator
title: Fix addition in calculator.py with live Codex
prompt: |
  The add function in calculator.py returns the wrong result.
  Update only calculator.py so that the tests pass.
workspace: ../calculator_bug/workspace
allowed_files:
  - calculator.py
required_checks:
  - pytest
doubt_points:
  - Keep the fix minimal.
oracles:
  - name: pytest
    kind: pytest
    command: -q
adapter: codex
codex:
  sandbox: workspace-write
  skip_git_repo_check: true
retry_budget: 2
tags:
  - python
